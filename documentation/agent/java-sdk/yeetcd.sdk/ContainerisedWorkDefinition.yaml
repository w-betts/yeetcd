# Documentation: ContainerisedWorkDefinition
# Generated: 2026-03-29T21:35:27.859Z

component_type: "class"
description: "Work definition for executing commands in existing container images. Allows specifying a container image and command to run."
name: "ContainerisedWorkDefinition"
responsibilities:
  - "Define container image and command for execution"
  - "Serialize container configuration to protobuf"
  - "Implement WorkDefinition interface for container-based work"
version: 1
dependencies:
  - "WorkDefinition"
  - "WorkContext"
  - "PipelineOuterClass"
interfaces:
  - description: "Creates a builder for ContainerisedWorkDefinition"
    method: "builder(String image)"
    returns: "Builder"
  - description: "Applies container configuration to protobuf builder"
    method: "applyTo(WorkContext workContext, PipelineOuterClass.Work.Builder workBuilder)"
    returns: "void"
  - description: "Returns empty stream (no native work definitions)"
    method: "nativeWorkDefinitions()"
    returns: "Stream<NativeWorkDefinition>"
implementation_notes:
  - "Immutable class with builder pattern"
  - "Uses Lombok for equals/hashCode and toString"
  - "Returns empty native work definition stream"
