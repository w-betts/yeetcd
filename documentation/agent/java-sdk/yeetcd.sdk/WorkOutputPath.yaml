# Documentation: WorkOutputPath
# Generated: 2026-03-29T21:36:32.035Z

component_type: "class"
description: "Defines an output path from a work item that can be consumed by downstream work. Maps a name to a container path."
name: "WorkOutputPath"
responsibilities:
  - "Define named output path from work execution"
  - "Map output name to container filesystem path"
  - "Serialize output path to protobuf"
version: 1
dependencies:
  - "PipelineOuterClass"
interfaces:
  - description: "Returns output path name"
    method: "getName()"
    returns: "String"
  - description: "Returns container filesystem path"
    method: "getPath()"
    returns: "String"
  - description: "Creates builder with name and path"
    method: "builder(String name, String path)"
    returns: "Builder"
implementation_notes:
  - "Immutable with builder pattern"
  - "Name is used to reference output in downstream work"
  - "Path is absolute path in container filesystem"
