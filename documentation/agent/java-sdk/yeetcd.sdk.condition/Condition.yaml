# Documentation: Condition
# Generated: 2026-03-31T20:41:14.983Z

component_type: "class"
description: "Abstract base class for all conditions that control work execution flow. Provides methods to apply conditions to protobuf builders with support for logical composition (AND, OR, NOT). Conditions determine whether work should execute based on context or previous work status."
name: "Condition"
responsibilities:
  - "Define base class for all condition types"
  - "Apply condition to work protobuf builder"
  - "Support logical composition via overloaded applyTo methods"
  - "Serialize condition to protobuf format"
version: 1
dependencies:
  - "PipelineOuterClass"
  - "EqualsAndHashCode"
interfaces:
  - description: "Applies this condition to a work builder"
    method: "applyTo(PipelineOuterClass.Work.Builder builder)"
    returns: "void"
    preconditions:
      - "Builder must not be null"
    postconditions:
      - "Condition is set in work protobuf"
      - "Builder state is modified"
  - description: "Applies this condition to an AND condition builder as left or right operand"
    method: "applyTo(PipelineOuterClass.AndCondition.Builder builder, boolean isLeft)"
    returns: "void"
    preconditions:
      - "Builder must not be null"
    postconditions:
      - "If isLeft=true, condition is set as left operand"
      - "If isLeft=false, condition is set as right operand"
  - description: "Applies this condition to an OR condition builder as left or right operand"
    method: "applyTo(PipelineOuterClass.OrCondition.Builder builder, boolean isLeft)"
    returns: "void"
    preconditions:
      - "Builder must not be null"
    postconditions:
      - "If isLeft=true, condition is set as left operand"
      - "If isLeft=false, condition is set as right operand"
  - description: "Applies this condition to a NOT condition builder"
    method: "applyTo(PipelineOuterClass.NotCondition.Builder builder)"
    returns: "void"
    preconditions:
      - "Builder must not be null"
    postconditions:
      - "Condition is set as nested condition in NOT"
  - description: "Converts condition to protobuf format"
    method: "toProtobuf()"
    returns: "PipelineOuterClass.Condition"
    postconditions:
      - "Result is non-null valid condition protobuf"
    invariants:
      - "Serialization is deterministic"
contracts:
  - "All conditions must be serializable to protobuf"
  - "Logical composition maintains condition semantics"
  - "Condition evaluation is deterministic"
invariants:
  - "Condition objects are immutable after construction"
  - "toProtobuf() produces valid protobuf for all condition types"
implementation_notes:
  - "Template method pattern: subclasses implement toProtobuf()"
  - "Overloaded applyTo methods support nested logical conditions"
  - "EqualsAndHashCode provided by Lombok for value semantics"
