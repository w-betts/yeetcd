# Documentation: CustomWorkDefinition
# Generated: 2026-04-10T22:10:03.689Z

version: 1
component_type: "class"
name: "CustomWorkDefinition"
description: "Abstract work definition for user-defined Java code. Subclasses are compiled, and execution ID is computed from class name and serialized state."
responsibilities:
  - "Provide abstract base for user work code"
  - "Generate deterministic execution ID for serialization"
  - "Support code execution in built container"
dependencies:
  - "yeetcd.sdk.NativeWorkDefinition"
  - "yeetcd.protocol.pipeline.PipelineOuterClass"
interfaces:
  - method: "applyTo(WorkContext containingContext, PipelineOuterClass.Work.Builder workBuilder)"
    returns: "void"
    description: "Sets custom work definition with execution ID on protobuf builder"
    preconditions: []
    postconditions:
      - "workBuilder has CustomWorkDefinition with executionId set"
  - method: "getExecutionId()"
    returns: "String"
    description: "Returns the unique execution ID for this work definition"
    preconditions: []
    postconditions:
      - "Returns SHA-256 hash based on class name and state"
implementation_notes:
  - "User extends this class and implements work execution logic"
  - "Subclass state is serialized to JSON for executionId generation"
  - "Execution ID enables container to load and run the correct class"
  - "Code generated by annotation processor invokes these at runtime"
contracts:
  - "executionId is deterministic for same class+state"
  - "Subclass must have no-arg constructor for instantiation"
invariants:
  - "executionId depends on class name and field values"
