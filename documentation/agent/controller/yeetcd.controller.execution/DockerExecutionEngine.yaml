# Documentation: DockerExecutionEngine
# Generated: 2026-03-29T21:34:48.320Z

component_type: "class"
description: "Docker-based execution engine implementation using docker-java client."
name: "DockerExecutionEngine"
responsibilities:
  - "Build container images using Docker daemon"
  - "Run jobs in Docker containers"
  - "Pull images on demand"
  - "Extract output files from containers"
  - "Stream container logs"
version: 1
dependencies:
  - "AbstractExecutionEngine"
  - "DockerClient"
  - "BuildImageDefinition"
  - "JobDefinition"
  - "JobStreams"
implementation_notes:
  - "Uses docker-java library for Docker API access"
  - "Creates temporary directories for output extraction"
  - "Pulls images automatically if not present"
  - "Extracts tar archives from containers for outputs"
  - "Removes containers after execution"
interfaces:
  - description: "Builds image using Docker daemon"
    method: "buildImage(BuildImageDefinition buildImageDefinition)"
    returns: "CompletableFuture<BuildImageResult>"
  - description: "Removes image from Docker"
    method: "removeImage(String image)"
    returns: "CompletableFuture<Void>"
  - description: "Runs job in Docker container"
    method: "runJob(JobDefinition jobDefinition)"
    returns: "CompletableFuture<JobResult>"
