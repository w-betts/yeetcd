# Documentation: AndCondition
# Generated: 2026-03-29T21:35:49.855Z

component_type: "class"
description: "Composite condition that evaluates to true only if both left and right conditions are true."
name: "AndCondition"
responsibilities:
  - "Combine two conditions with AND logic"
  - "Evaluate both conditions in work context"
  - "Deserialize from protobuf format"
version: 1
dependencies:
  - "Condition"
  - "WorkContext"
  - "WorkResultTracker"
  - "PipelineOuterClass"
  - "Conditions"
interfaces:
  - description: "Evaluates both conditions with AND logic"
    method: "evaluate(WorkContext workContext, WorkResultTracker workResultTracker)"
    returns: "boolean"
  - description: "Creates AndCondition from protobuf"
    method: "fromProtobuf(PipelineOuterClass.AndCondition andCondition)"
    returns: "AndCondition"
implementation_notes:
  - "Java record (immutable)"
  - "Short-circuit evaluation (left && right)"
  - "Used for combining multiple conditions"
