# Documentation: yeetcd.controller.source
# Generated: 2026-03-31T20:40:51.131Z

component_type: "package"
description: "Source code extraction and building. Handles extracting source code from zip archives, parsing yeetcd.yaml configuration files, and building source code using configured build images and commands."
name: "yeetcd.controller.source"
responsibilities:
  - "Extract source code from zip archives"
  - "Parse yeetcd.yaml configuration files"
  - "Build source code using containerized build environments"
  - "Cache build results based on source SHA"
  - "Manage artifact definitions and output directories"
version: 1
dependencies:
  - "yeetcd.controller.execution"
  - "jackson (YAML parsing)"
  - "apache-commons-compress (zip extraction)"
subcomponents:
  - "SourceExtractor"
  - "SourceBuilder"
  - "Source"
  - "SourceExtractionResult"
  - "SourceBuildResult"
  - "YeetcdDefinition"
  - "ArtifactDefinition"
  - "SourceLanguage"
  - "ZipExtractor"
contracts:
  - "Source extraction produces valid directories with yeetcd.yaml files"
  - "Build caching uses source SHA as cache key"
  - "Build commands are executed in isolated containers"
  - "All artifacts defined in yeetcd.yaml are captured after build"
invariants:
  - "Extracted source directories are temporary and marked for cleanup"
  - "Build cache is bounded (TODO: implement size limit)"
  - "Source SHA uniquely identifies buildable source state"
