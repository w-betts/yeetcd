# Documentation: yeetcd.test
# Generated: 2026-03-31T20:40:52.841Z

component_type: "package"
description: "Pipeline testing utilities for unit testing without container execution. Provides in-memory simulation of pipeline execution with configurable work results for fast, deterministic tests."
name: "yeetcd.test"
responsibilities:
  - "Provide FakePipelineRunner for testing pipelines"
  - "Simulate work execution with configurable results"
  - "Track work execution stages"
  - "Evaluate conditions in test context"
  - "Support work result matching based on work and context"
version: 1
dependencies:
  - "yeetcd.sdk"
  - "yeetcd.sdk.condition"
subcomponents:
  - "FakePipelineRunner"
  - "FakePipelineRun"
  - "FakePipelineRunResult"
  - "FakeWorkResult"
  - "FakeWorkMatcher"
  - "FakeWorkMatcherResult"
  - "FakeWorkExecution"
  - "FakeSimpleWorkExecution"
  - "FakeCompoundWorkExecution"
  - "FakeWorkExecutionStage"
  - "FakeWorkStatus"
  - "FakePipelineStatus"
contracts:
  - "FakePipelineRunner produces deterministic results"
  - "Work execution respects dependency order"
  - "Conditions are evaluated using production logic"
  - "Specified work results take precedence over defaults"
invariants:
  - "Work execution is idempotent within a test run"
  - "All work items are tracked by WorkKey (work + context)"
  - "Execution stages group parallelizable work"
